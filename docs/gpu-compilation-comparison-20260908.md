Qwen3.5-9B 编译执行的真实 GPU 对照，2026-09-08

已经确认一项大幅、可复现的效率损失：当前 eager 配置关闭编译和 CUDA Graph；在同一 Qwen3.5-9B、同一非零 LoRA、相同输入及相同输出长度下，开启官方编译配置使所测生成调用快 **2.58–6.59 倍**。LoRA 执行方式保持为运行时适配器。这是当前配置变化的受控比较，不能把历史不同任务／适配器的 3.84 倍差距全部定量归因于它。

每条请求输出固定为 256 token；单路和四路分别输出 256、1024 token。每个形状先预热，再测两次清空缓存和两次复用前缀，表中为两次调用的平均秒数。计时包含预填充和主机开销。

| 输入与并行请求 | 清空缓存：eager → 编译 / 秒 | 耗时比 | 复用前缀：eager → 编译 / 秒 | 耗时比 |
|---|---:|---:|---:|---:|
| 2,048 token，单路 | 34.73 → 5.43 | 6.40× | 34.59 → 5.25 | 6.59× |
| 2,048 token，四路 | 36.41 → 6.51 | 5.60× | 35.77 → 5.80 | 6.17× |
| 24,576 token，单路 | 38.05 → 8.59 | 4.43× | 34.64 → 5.43 | 6.38× |
| 24,576 token，四路 | 49.37 → 19.13 | 2.58× | 35.65 → 6.61 | 5.40× |

两边实际缓存命中相同：清空时均为零，复用时每条分别为 1,584 或 24,288 token。两边模型／适配器／分词器、输入、采样和运行库指纹一致，完整引擎参数差异仅为 `enforce_eager` 与 capture sizes `[1,2,4]`。实际日志确认完成 PIECEWISE 和 FULL CUDA Graph 捕获；不以配置声明代替真实执行。首次启动为 77.48→273.58 秒，编译增加约 196 秒的一次成本；没有把这部分算进已预热调用，也没有声称整段训练会达到同样倍数。

概率与训练正确性仍需分别验证

包含重复条件的 40 对请求中，有 7 对完整输出相同，其余出现 token 分歧。只对分歧前相同历史的 4,250 个 token 比较选中 token 的 logprob：绝对差中位数约 0.000054，95% 分位数 0.06970，最大 0.47992。后续偶然相同的 token 没有拿来比较。该结果不能证明逐位一致、全分布误差上界，或实际 actor／rollout 的重要性权重；两边均为 BF16，也没有用量化设置变化解释结果。

已从第 5 步原生检查点启动官方编译配置恢复验证，运行目录为 `/home/chen/runs/inference-efficiency-20260908/resume-compiled-02`。实际 FSDP 日志已确认加载 496 个 LoRA 键、优化器、训练侧 RNG 和学习率调度器；训练全局步数恢复为 5。恢复后实际出现的前三个任务与保存 sampler 的行号 496、324、306 一一对应。

恢复后的第一个反事实任务，四条都提交 `[0,1]`，质量均为 0.511777；第二个中介任务也未产生可保留组。第三个反事实任务产生质量差异，已完成真实第 6 次更新：梯度范数 0.1225586，Token-TIS 均值 1.0000582，有效样本比例 0.9989741，上端裁剪比例 0.000035113（约 0.00351%），没有非有限指标。官方日志记录了 actor 更新、检查点保存和随后权重同步；这是一轮实际执行验收，不是所有输入、所有后续更新均正确的证明。

第 6 步总耗时 1,399.67 秒（23.33 分钟），其中生成 1,311.04 秒（21.85 分钟），actor 更新 56.18 秒，权重同步 6.87 秒。该步生成了三个题组才保留一个，与此前只生成一组的首步不是相同工作量；也不能拿它直接除历史平均数当作编译提速比例。被保留组的最长单轨迹生成为 621.40 秒，环境工具调用最长仅 1.53 秒。当前批次任务难度、轨迹长度和过滤次数仍会显著影响每次有效更新的耗时。

被保留的反事实题 `SAMPLED-2000334` 真值区间为 `[0.790834, 0.887028]`，四条答案为 `[0.85,0.95]`、`[0.5,0.7]`、`[0,1]`、`[0.2,1]`，端点平均误差分别为 0.06107、0.23893、0.45190、0.35190。其奖励可以区分这些实际答案；全部轨迹均未触发长度惩罚。该证据纠正“前两组被过滤，所以所有反事实都没有信号”的过度推断，但尚未证明更新后在同题或留出集上学得更好。

实际第 6 步完整环境事件、四条保留输出和 Ray 进程日志在 `resumed-first-groups-evidence.tar.gz`，SHA-256 为 `3468699cd20632dcafd4cd5b727fb0ec3b83f6b14e26ba6c444c4c0c3b33ffaf`。文件名沿用采集开始时的名称；03:53 的已完成快照确实包含第 6 步更新，不能继续把它描述成仅有待完成生成。

04:01 进一步核对并保全了原生 `global_step_6`：496 个 LoRA 张量全部有限，且均较第 5 步发生改变；496 个 Adam 状态的 step 均为 6，992 个矩均有限，调度器计数为 6。数据游标从 6 批变为 9 批，与本次实际生成三组一致；恢复后续八行 `[461,178,81,158,366,437,14,75]` 与不间断 sampler 相同。文件哈希与只读验收见 `research/inference_efficiency_20260908/resumed-update6-native-acceptance.json`，原生副本在服务器 `resumed-update6-acceptance/global_step_6`。这确认实际参数更新及早期恢复连续性；没有以 CPU 文件核对声称 GPU 内存恢复逐位一致，也没有声称已修复跨 epoch 的已知恢复缺口。

原始两组 GPU 日志、全部 token／logprob、计时、比较结果和实际恢复启动快照保存在 `research/inference_efficiency_20260908/gpu-compilation-evidence.tar.gz`，SHA-256 为 `21a2245724cce10122ed1217e191a11091af7e0c0adf40b8bca5956cc242c745`。图表见同目录 `figures/compilation-speed.png`。基线使用此前已逐位核对的 run-02 非零适配器；恢复训练使用本轮第 5 步完整原生检查点，两者用途不同。原生权重继续保存在服务器。
