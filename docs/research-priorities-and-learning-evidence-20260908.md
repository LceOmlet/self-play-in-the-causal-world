研究优先级与实际学习信号，2026-09-08

最新状态与选题覆盖下文：训练继续，同步与后台额外验证均按用户要求关闭；下文第 50 步验证计划已取消。主问题仍是旧训练约 800 步无明显提升所触发的“任务有无价值、模型能否学进去”，而不是独立追求防止高估。当前没有证明评分高估是主要学习障碍。补齐基准／任务误差作为一次性辅助工作收束，不继续扩展为研究主线。当前下一步归并到 B/E 与 D：从已有合法信息、实际统计推断和同题奖励差异中辨别有效解法为何难以产生或难以被强化；沿有全局影响且有明确改法的新证据推进，不重复下文已足够的错误案例。

当前选题需要纠偏。能够证明一个性质，并不说明它是项目的决定性问题。此前反复扩展最坏情形、观察等价世界和参数支持边界，容易把“证明容易做扎实”替代为“最影响当前学习”。这些工作可以保留其适用结论，但不能在缺少实际影响证据时持续占据主线。本轮准备的覆盖边反转诊断没有执行，候选脚本已撤回；没有据此生成新题或修改训练环境。

接下来按三个依据选择工作：实际影响了多少任务或轨迹；源码、张量或已发生的数据能否确定机制；确认或修复后是否会改变训练、实现或评估的决策。暂时没有第三项的数学扩展降级。相同失败机制已有确定证据后，不继续搜集更多同类反例。保留必要的独立参照和测试，不把全面重跑当作每个只读分析的默认成本。

| 顺序 | 已有证据与实际意义 | 下一步会改变什么决策 |
|---|---|---|
| 执行错误优先阻断 | 动作掩码被工具 token 覆盖、保存／恢复步数错误此前已有实际证据并修复；官方数值更新路径保持不变 | 如出现新的掩码、梯度、奖励归属或恢复错误，立即修复；不反复用同一条已通过路径证明“全局正确” |
| 当前主线：模型能否学会有效解题 | 固定基座／第 25 步尚无整体收益；训练中有较多答案，但严格正确及有可核对因果计算的成功仍少 | 沿现有官方同题验证检查完成、真实误差和解法变化，区分探索不足、错误公式、算术／汇总错误与解码行为，再决定需要修什么 |
| 能力判别的限制：生成条件与观察捷径 | 纯观察第二名在原冻结强反转部分为 57/80，旧主动程序为 51/80，原弱基线不能证明因果方法收益 | 用强基线防止把奖励提高误报成因果能力提高；尚不能认定排名偏好是当前训练无改善的主因，不直接改配额或奖励 |
| 反事实核与预算：按实际影响继续 | 74 道冻结题仍有 23 道未认证；当前已提交题使用已认证真值，失败题不冒充可信标签 | A/A2 的全局认证义务继续，但它们不自动阻断已认证题上的训练；有明确等价表示和收益时回到内核修复 |

这不是按“容易测试”缩小长期目标。A/A2、C/C2、D、B/E 与最终 10000 次实际官方更新要求均保留。优先级决定当前资源投入；正确性修复仍须有源码依据、独立参照和适当覆盖。只读分析不改变原奖励、预算、CPT 或 DAPO。

前 39 次更新的完整记录已复用既有读取器和固定官方优势函数核对，共 45 个生成题组、39 个保留题组、156 条保留轨迹；过滤六组为原训练行 `[478,496,324,352,318,53]`，不是“大部分样本被过滤”。153/156 条保留轨迹提交了答案。由于 45 组共生成 180 条，即使其余 24 条全部未提交，前 45 组的提交数也至少是 153/180；这只是数量下界，不把保留集合当作未筛选总体。

| 家族 | 保留轨迹 | 提交答案 | 严格正确 | 严格正确且正优势 |
|---|---:|---:|---:|---:|
| 后门调整集 | 36 | 34 | 4 | 4 |
| ATE | 28 | 28 | 不定义二值阈值 | 不定义二值阈值 |
| 中介及顺序 | 28 | 28 | 1 | 1 |
| 反事实区间 | 24 | 24 | 不定义二值阈值 | 不定义二值阈值 |
| 最佳干预 | 40 | 39 | 10 | 10 |

这与原始基座贪心验证的 13/25、第 25 步的 11/25 提交数差别明显。但训练／验证世界、采样温度及动态筛选不同，不能用这项比较证明温度导致失败、训练过拟合或分布错误。它足以否定把所有运行笼统归为“上下文导致无法完成”的说法。接下来的同题验证必须继续报告完成率及真实任务误差，不能只看训练奖励。

九个强反转保留组共 36 条，七条动作正确；前三十步之前的四条已检查，本轮完整检查新增三条，而非挑一条有利例子。

第 32 步、原训练行 383 的成功轨迹进行了 16 次合法干预，最后主要引用 `P(ETJ=3 | KNI=0, do(IOV=0))=11/16` 支持部署动作 0。该条件概率代入精确总体为 0.7445755，实际部署值 `P(ETJ=3 | do(KNI=0))` 为 0.2480274；实际 IOV 同时是 KNI 和 ETJ 的父节点。选对动作没有证明两个量相等，亦不能用“做了很多干预”认证因果计算。

第 34 步、原训练行 408 的一条成功轨迹，直接合并四个 `do(VER=v)` 实验后计算 IRL 条件比例。完整反馈实际为 `[48/99,57/148,4/9]`，原文第三项最终仍写成 `5/11`。按同样实验比例代入精确总体得到 `[0.4460257,0.4492962,0.3330729]`，会选动作 1；真实部署值为 `[0.4751990,0.4259393,0.4258252]`，动作 0 最优。当前混合条件统计量并不一致估计部署目标。

同题另一条成功轨迹仅做两批 64 个自然样本。完整反馈的成功数／总数是 `[13/40,29/84,1/4]`，观察比较应选 1；模型原文写成 `13/25、15/39、1/2`，因算错而选到真正最优的 0。这是实际正优势轨迹中的计算问题，不是新构造的难题。

以上三条不能推出整个策略不可能学习，也不能推出终局奖励或 DAPO 算法有错误。强化学习依靠分布上的预期结果改进，单次误算恰好得奖并非矛盾。实际需要辨别的是后续成功是否越来越来自可复用的正确计算；目前缺少这种能力提高证据，不能用终局正确数、非零梯度或更快生成替代。

操作记录：用户本轮重申停止后，连续验证已按进程身份停止于已发布的原生第 39 步，未完成的下一步不计入更新。用户随后明确允许边训练边找改进，已按最新授权恢复。原生 LoRA／Adam／调度器计数均为 39，数据生成进度为 45；实际官方 CPU 加载恢复出的后续八行是 `[98,399,30,9,62,170,61,358]`，与连续 sampler 一致。这里仅 GPU RPC 被 CPU 审计替代；训练仍由原官方入口执行。GPU 日志随后实际确认加载 496 个 LoRA 键、优化器、RNG 和调度器。

新运行目录为 `/home/chen/runs/heldout-learning-20260908/resume-after-user-hold-01`，模型仍为 Qwen3.5-9B，已启用编译／CUDA Graph。当前验证沿用原有 500 次目标与原截止时刻，不重置为另一场实验；最终 10000 次运行及不设更小截断要求继续保留。后续研究默认与训练并行，除非出现确定的执行正确性问题或明确的新停止指令，不因理论分析或一次观察超时停止训练。

归档时已实际完成恢复后的第 40 次更新：原生进度记录为 40 次更新／47 个生成组，第 40 步官方日志为两个生成组、梯度范数 0.171875、Token-TIS 有效样本比例 0.9989072，并写出四条保留轨迹。上文能力与信号统计仍冻结到第 39 步，不把这次恢复更新当作能力提高。

证据与复现入口：`research/training_hold_20260908/README.md`，其中保存原始过程、三条完整新增成功轨迹、逐批计数、官方优势核对和恢复身份。没有执行覆盖边反转实验，不能把之前的准备叙述当作其结果。

结构轨迹补充检查现已收束，详见 `structural-answer-signals-20260908.md`：全部 64 条保留结构轨迹中，中介 14/28 条把治疗变量祖先误列为中介，其中 11 条实际做过相关干预；不能归因为完全不会查询。两条非空严格正确的结构答案都有错误推理，后门一条更把真实无效干预的样本波动及错误计数当作父节点证据。它们限定了“终局正确”的解释范围，没有证明 DAPO 或奖励错误。这批证据已足够，不继续扩展同类反例。训练继续，下一项主要证据是既有第 50 步固定验证的完成率、真实误差与方法变化；不得预先把它称为通过或失败。
