持续生成训练题恢复记录

用户没有要求把训练限制为固定 500 题。接入官方 verl 时，为冻结隔离验证的输入，错误地把训练数据也导出为 500 行 parquet，并使用 shuffle 和多 epoch 重复遍历。这是项目接入层引入的任务供应改变，不是 DAPO 的必要条件。冻结可复核证据不应改变训练的持续生成语义。

全局影响：重复固定世界会把训练从不断接触新 DAG/CPT/问题，改变为对有限题库的重复优化；即使每个 epoch 打乱顺序，也不能恢复新任务供应。此次发现时运行仍在第一轮：第 74 次实际更新、尚未取满 500 个题组，不能据此声称固定题库已经造成过拟合，也不能把这个新接入问题解释成此前 800 步训练的原因。修复直接消除后续循环回旧题的行为。

实现复用原 `BalancedTrainingRowStream` 生成逻辑，保留原家族轮换、最优干预生成和反事实认证／拒绝规则。只把隐式生成器位置提取为可恢复游标，并通过官方 `data.custom_cls` 接入 `RLHFDataset`。官方取行处理、同题四轨迹、DAPO 动态过滤、优势、损失及更新实现均未替换。

每次需要一个新的题组才生成一个新世界，先把完整题目、已认证真值、转译行和生成器后继游标原子写入 journal，再交给官方循环。重复索引或断点恢复读取同一记录；过滤掉的题组仍推进生成游标，下一题重新生成。`sys.maxsize` 仅为 Python map-style 接口的逻辑索引空间；没有对应大小的题库，也没有 500 题循环。必须使用顺序采样、单进程数据加载；配置不符会直接拒绝。新适配器暂用同步供题，未增加预取线程。

恢复采用显式数据迁移：保留最新完整检查点的实际更新数 U、累计生成数 G，逐文件保留模型、Adam、调度器和 RNG；只替换数据加载器状态与对应 N／epoch 元数据，并存下旧数据状态。新任务流使用独立起始 seed 4,000,000，逻辑索引从旧 G 接续。停止旧进程后再次读取最新检查点，避免在复制期间刚完成的更新被遗漏。任务分布本身不因新 seed 改变。

CPU 验收已通过：

- 原生成器游标 17 项测试（含 17 个子测试），与改动前生成的完整行摘要对照，覆盖拒绝重采样与各家族断点。
- 适配器 15 项测试；真实五家族全部经过原生成器、原转译函数、官方 `__getitem__` 和持久化恢复，恢复不再生成或求解 CF。这一轮生成共 1.299 秒，仅代表该五题，不代表总体速度。
- 数据迁移 7 项测试；执行真实官方基类及 DAPO `_load_checkpoint`，只模拟 GPU RPC。
- 官方 `fit/save/load` 6 个 CPU 场景：过滤下 G/U 分离、四轨迹同题、分段与连续题序一致、journal 超前恢复、索引 499→502 不回旧题、完成后不取题及连续 10 组恒定奖励的官方报错。内容与 GPU RPC 是测试夹具，不能据此声称模型学得更好。
- 隔离 checkout 使用已保留第 50 步完成配置解析、真实迁移和官方来源预检，未启动额外 GPU 验证。

实际迁移保全至 U77／G86，10 个 actor 文件在保全源和迁移副本逐字节相同。首次 GPU 恢复已实际载入 LoRA、Adam、RNG 和调度器，但第一题交付前因 Ray 工作进程中另一个 `scripts` 包遮蔽原转换文件而退出，尚未产生新行或更新。先前 CPU 验收没有覆盖这个工作目录／包名条件，不能把首次启动记为恢复成功。失败日志和退出状态完整保留在 deployment/。

已把转换函数导入改为已指纹核对的绝对文件路径，复用原函数，不复制转换逻辑。新增回归先复现外部 `scripts` 遮蔽下旧导入失败，再通过实际官方文件类加载和真实五家族往返；在官方 verl 工作目录中 16 项 CPU 测试全部通过，原始日志保留为 converter-import-cpu.log。随后真实 Ray CPU 工作进程使用本地 Qwen3.5 tokenizer／processor、官方工作目录及生产 PYTHONPATH 顺序，确认旧导入确实被官方 verl/scripts 包遮蔽，而修复后五家族全部通过官方取行；CF 实际子进程返回认证真值，未加载模型或使用 GPU，见 ray-dataset-probe.json。

生产已从同一 U77／G86 成功恢复至 `continuous-training-restore-20260908/resume-stream-02`，主进程 712391。真实 GPU 日志确认载入全部 LoRA、Adam、RNG 和调度器。首个新逻辑索引 86 实际生成 seed 4,000,000 的 ATE 世界，四条真实环境轨迹共享该新题及随机带。早期交付快照见 deployment-02/。

随后原生第 78 次更新已完成并保存，G 推进至 87；四轨迹均提交答案且未超长，496 个 LoRA 张量均有有限的实际参数变化。保存的数据游标与 journal 前缀一致，下一道 seed 4,000,001 的 CF 题已生成并缓存完整认证区间，未因更新回到旧题。`update78/` 保存真实检查点数据状态、完成轨迹、下一题记录及只读验收脚本。本项已跨过实际更新边界，证明连续供题和更新接续工作；一组更新不证明能力改善。

后续实际部署：持续流已推进至 U96／G110，并在这个完整检查点边界迁至 `long-run-10000-20260908/resume-01`。保留生成器 seed、游标、已有题目／真值和训练状态，同时接入已验收反事实内核，将目标改为累计 10,000 次实际更新并取消墙钟截断。首个正常第 97 次更新已保存，累计生成 G111、journal next=112；运行配置实际绑定 `stream.json` 与 `CPTWorldStreamingDataset`，没有重新使用固定 parquet。GPU 日志确认恢复 LoRA、Adam、RNG、调度器，学习率仍为 1e-6；这是实际接续证据，不是能力提升证据。

所有额外模型验证仍关闭；连续供题修复不改变奖励、预算和官方算法。检查点恢复依赖对应完整 journal，备份时须一并保留。最新部署证据见 `research/long_run_prepare_20260908/deployment/`。

源码与复核入口：`src/cpt_world/verl_streaming_dataset.py`、`src/cpt_world/trl_environment.py`、`scripts/migrate_dapo_continuous_data.py`、`research/continuous_training_restore_20260908/`。旧 500 题档案仅保留为历史输入与诊断证据，不再用作当前持续训练的数据源。
