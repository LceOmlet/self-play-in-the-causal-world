正常训练记录与持续供题，第 100 次更新

旧固定 500 题来自接入层未经要求的决定：冻结验证输入被错误地扩展到训练。它不是官方 DAPO 的要求。当前已经恢复原生成器持续供题；维护仓库中的旧 prepare/control 命令行入口也已关闭，直接调用不会生成题库或操作训练进程，导入复用的函数保持不变。训练目标仍为累计 10,000 次实际更新，额外模型验证关闭。

现有正常训练证据覆盖 100 次更新、115 个完成题组、460 条回答，其中 400 条进入官方更新。旧固定题库仅使用第一轮的 86 个题组；恢复后的 29 组为持续生成的新题，五类分别 6、6、6、6、5 组，没有每类 100 的封顶。生成日志中超前于完成边界的任务不计入完成样本。

连续段 113/116 条提交答案；后门合法调整集为 7/24、最佳干预最优动作为 9/24、中介集合与路径边同时正确为 0/20。ATE 的平均 TV 误差为 0.15572、反事实端点 MAE 为 0.16136；对应同题总体观察参考为 0.09166、0.02499。参考来自已有终局记录，不是新模型验证。这些只有 29 个世界且任务不断变化，不能当作基座对照、学习增益或确定不可学的证据。

离线读取器新增显式生成序号目录，可衔接有限历史与持久化任务 journal；不再假定完成训练始终在固定题库第一轮。图表区分更新与生成题组横轴，并标出持续供题和内核迁移边界。缺失最终答案的样本保留在全采样得分和严格失败比例中；数值误差保持缺失。缺少最终指标日志的第 50、95、96 步不推算聚合奖励或耗时。旧第 51 步报告的所有组、奖励、误差、计数和排除项与原结果一致。

本次改变只涉及历史入口防误用、离线报表和工作区代码同步，没有修改任务、预算、奖励、生成器或官方 DAPO。完整逐题快照、来源摘要和图表保留在本地／服务器 research/training_progress100_20260908/，不随公开源码上传。当前仍需从正常训练过程判断有效解法能否得到强化；没有将补齐统计当成学习改进。
