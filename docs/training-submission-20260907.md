轨迹核对与官方 DAPO 训练提交，2026-09-07

用户最新指令是核对几个真实轨迹：确认观察与干预会改变答案、差别有实际幅度、存在预算内可计算的解法，并核对强化学习实现后提交训练。已按此停止扩大基座评估，完成数据及接口验收，从原始 Qwen3.5-9B 提交官方 verl / verl-recipe DAPO。此前“生产长训练停止”的状态由本次明确授权覆盖；旧检查点仍不用于初始化或能力证据。

随后用户要求：所有剩余问题解决后，最终正式训练完整跑到 **10,000 次更新**，不设额外的较小步数截断。最终运行将取消本次验证的 500 步／24 小时限制，并核对 epoch 耗尽与动态过滤不会导致提前结束。本报告记录的当前运行及其冻结启动参数仍是验证阶段，不在运行中悄悄改写为最终训练。

两个实际强反转任务

以下取自已冻结的真实任务及公开交互记录。固定程序仅根据公开提示和合法工具反馈解题，答案锁定后才与隐藏真值比较。它是可行解法的见证，不是最优样本效率证明，也不是模型已经学会的证据。完整队列（含失败）仍保存在 `research/active_decision_diagnostic_20260907`。

| 任务 | 观察选择 → 因果正确选择 | 沿用观察选择的因果损失 | 合法程序轨迹 |
| --- | --- | ---: | --- |
| SAMPLED-20034915273-best_intervention-decision-a12 | state_0 → state_2 | 0.0791994，约 7.92 个百分点 | 27 次工具调用，消耗 16,383 / 16,384 标量预算，正确 |
| SAMPLED-20032513229-best_intervention-decision-a0 | state_1 → state_2 | 0.0486149，约 4.86 个百分点 | 33 次工具调用，消耗 28,669 / 28,672 标量预算，正确 |

第一题各动作的因果值为 [0.177637, 0.730968, 0.098438, 0.421756]；第二题为 [0.452489, 0.421936, 0.470551]。两题分别是最小化与最大化目标。正确选择由目标方向决定，不能一概取最大值。程序通过干预筛选和有效后门调整得到正确动作。仅重建命令与反馈的上下文分别为 5,999、6,740 tokens，未计模型额外推理文本；这证明这些公开记录可以较短，不保证任意模型执行都能在 32K 内完成。完整数值及记录哈希见 `research/training_submission_20260907/task-value-witnesses.json`。

一条实际基座模型轨迹

基座评估的首题是 `SAMPLED-200000-ate-target_query-a1`，目标 GED → VPK。停止前有一条完成轨迹，ID 为 `7a8a9289e2c74de280e3fdc41521411c`；另外三条被中断，不能记作失败或零分，也不能据此估计组内方差或成功率。

完成轨迹共有 23 次有效实验和一次终局提交，消耗 2,848 标量预算。第一次 `observe [GED,VPK] n=128` 中 GED 两态样本数是 **4 与 124**。模型最终提交

`[-0.06451613, 0.14516129, 0.00806452, 0.13709677, -0.22580645]`。

逐项用首个公开直方图的整数计数计算，终局向量与第一次未经调整的经验条件差完全吻合到输出精度（最大差 < 5e-9）。但在之后第一次 `do(SUM=state_0)` 的 64 个样本里，GED 两态已变为 20 与 44。即模型确实取得了改变状态覆盖的实验数据，最终数值却等于最初仅四个对照样本支撑的观察估计。这是可重放的具体错误，不能声称读到了模型的隐藏推理，也不能外推为全部模型错误的原因。

真实因果效应为 `[0.02744676, -0.00508797, 0.00033208, -0.01449803, -0.00819284]`。终局总变差误差 **0.3095765**，实际原始奖励 **0.0848464**，终局只计奖一次。此轨迹给出的直接学习目标是利用已获得的实验信息、处理不均衡覆盖和正确估计效应，而不是仅增加查询次数。复核脚本为 `research/training_submission_20260907/analyze_case.py`；原始事件、终止记录和中断轨迹均归档。

官方强化学习实现的验收范围

完整训练路径继续调用固定版本官方 `dapo.main_dapo`，不含 RLOO、TRL 训练器或自写替代损失。官方 verl 基线为 `23af6a7a2e8d6efeeb2adbe5d1689c7a24f503a3`，recipe 基线为 `ee3aef1690d6bb5e6448052c4842e8efb7a3f76c`。两处已审阅的接口补丁处理工具终止与保留动作掩码，详见 `dapo-execution-audit-20260907.md`。

此前 run-02 的实际模型更新已核对终局计奖、动作与工具 token 掩码、组优势、动态过滤、官方 Token-TIS、token-mean loss、梯度和 Adam 首次更新。此次启动前再次通过来源校验及 **59 项相关测试**（32.02 秒，6 条可选后端警告），并固定实际源文件和数据哈希。启动沿用已验收的每组四条轨迹、每次保留一组、microbatch=1、LoRA rank=8、学习率 1e-6 配置。

这些是具体执行路径的正确性证据，不能证明所有未来输入、未触发分支和数值后端永远无缺陷，也不能推出能力必然提升。官方标准化 DAPO、裁剪、长度处理及 Token-TIS 的代理目标也不能宣称等于原始平均环境奖励的无偏梯度。当前没有发现阻止这条已验收路径训练的确定实现错误；能力收益要由实际训练及任务误差验证。

实际提交的数据与运行

- 使用现有完整生成器：500 道训练题，每家族 100 道；25 道固定验证题，每家族 5 道。最优干预仍为原有一致／强反转比例，训练 20/80，验证 1/4。未改奖励、任务配比、预算、强反转阈值或反事实求解超时。
- 525 个实际 `world_source` 指纹全部不同，训练与验证无重叠。所有入选真值及满分终局接口检查通过；非反事实题重算真值，反事实题复用刚完成的原求解器认证并核对认证状态与端点评分。
- 反事实共认证入选 105 道；原生成器还拒绝了 37 次尝试（25 个 RuntimeError、12 个资源限制异常），全部保留。这不代表反事实内核全面解决；训练范围是原生成器接受且真值已认证的任务。
- 此数据中强反转题的最小因果损失为 0.0461282。它是本次数据统计，不是整个生成分布的下界证明。
- 新运行：`/home/chen/runs/training-submission-20260907/run-01`。原始基座初始化全新 LoRA，不加载历史适配器或恢复检查点。
- 最多 500 次官方更新，且 24 小时墙钟上限；每 5 步保存，每 25 步在固定验证题上评估。这些是执行配置和资源上限，不是学习充分性的判据。每次只保留一个四轨迹题组，不能把 500 步等同于论文规模的 500 步。
- 为执行用户“分析几个轨迹后提交”的指令，没有先等待全量基座评估。因此固定验证集目前缺少完整基座结果；后续能力比较仍须补齐同题基座，不能把停止的部分评估当作完整前测。

启动源码 HEAD 为 `3f83d21dbe925d8c3e37f2242d391e65e2302840`。后续本次提交仅增加研究脚本、说明和证据，生产源码、算法与配置在运行中不变。主进程 PID 247603，创建时间 1788793685.17；启动约 638 秒时身份核对仍存活，已有 79 个动作事件及一个后门任务终局计分，四条轨迹均已交互。此处尚未确认首次参数更新，不能据此报告模型改善。

归档入口为 `research/training_submission_20260907/README.md`：完整数据、被停止的基座评估、启动前验收和训练启动快照分别保存并计算 SHA-256。训练启动快照不是训练结束日志，后续训练结果另行保存。长期 goal 保持 active；继续核对真实任务误差、有效组组成、截断与更新，并保留反事实认证及生成／预算问题的剩余义务。
