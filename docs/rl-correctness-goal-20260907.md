强化学习正确性、环境判别性与效率：持续推进 Goal

用户于 2026-09-07 授权将可分析、可明确改进的问题归并成 goal 并持续推进。对应线程 goal 已建立。本文件是持续更新的工作清单；创建清单不代表完成工作。

长期目标是：任务与奖励确实区分目标能力，任务在声明的语义、精度和预算下成立，训练真实使用官方 DAPO 并正确接收环境信号，计算效率不依赖放松答案、改变任务比例或隐藏失败。生产长训练保持停止；旧检查点不作为可信证据。受控训练验证从原始基座开始，在被验证任务和已核对接口上隔离执行。

已验收基线：`kernel-compilation-20260907.md`；74 道冻结反事实题中 51 道成功认证；90 项相关测试、22,183 项子测试通过；官方 verl / verl-recipe 来源与实际导入路径校验通过。完整证据位于服务器 `/home/chen/runs/kernel-integration-20260907`。

| 优先级与工作项 | 归并关系及确定事实 | 下一项工作与验收依据 | 状态 |
|---|---|---|---|
| P0 / A：反事实全局认证 | 原三节点 [0,2/3] 题已由保持联合可实现性的精确化简解决；74 题仍有 23 题未认证，不是仅由节点多或内存大造成 | 继续推进其余结构的等价优化表示与有效全局界；解析/有理数参照、现有回归及冻结题重放，分类记录收益与退化 | 适用链已修复，其余继续 |
| P0 / B：官方 DAPO 执行正确性 | 来源验证、正确实现、代理目标适配性分别验收。终止协议、动作掩码修复与官方 Token-TIS 的实际一次模型更新已通过 | 当前轨迹→loss→Adam 路径已核对；继续明确未实际触发分支、任务分布和长期目标的适用性，不将单次更新扩大为全局保证 | 当前一次执行通过，整体未完成 |
| P1 / A2：符号计算和内存 | 与 A 同属反事实内核，但构造阶段不同于最优化阶段。第 43 题首个消息 360,000 单元、3,240,000 求和项；4/6 构造失败题没有无关共享祖先 | 分析真实因子作用域、消息乘积与可共享表达；证明等价后减少物化和重复计算；记录单元数、实际建模耗时/RSS及全局认证变化 | 已定位，待改法 |
| P1 / C：最优干预与强反转生成 | 原第 1、3 项合并。强反转低接受率和最终结构/CPT 分布变化都来自同一跨 CPT 联合条件；此数值筛选仅用于 best_intervention，ATE 不经过它 | 根 CPT 切片、正确条件权重及混合状态数一般密度已证明；下一步从必要条件省去无用推断，或构造密度正确且有确定效率收益的条件提案 | 切片、一般密度、弱结果界已验证；提案未完成 |
| P1 / C2：DAG/CPT/任务链路 | ANOVA 等价投影已验收；结构核的最小后门调整集基数可严格转为带禁用节点的整数顶点割，无需组合枚举 | 顶点割已通过独立活跃路径穷举与 109 项回归、27,855 项子测试；480 个角色列表、20 道最优干预及 25 道全类型输出/提案序列完全相同，所测生成耗时再减约 30%–32% | 投影及最小基数内核改进已验收，随 C 继续 |
| P1 / D：任务可判别性与预算 | 与生成条件、答案定义相关，独立于求解器认证耗时。全 CPT 参数数不能替代统计难度 | 根据实际合法动作、按测量变量收费、可达上下文、条件事件分母和任务容许误差推导必要/充分条件；弱边最坏情形证明不能当作总体比例 | 尚无完整校准 |
| P2 / E：训练验证与可视化证据 | 奖励曲线、任务误差、动态过滤后的任务组成属于 B 的验收，不替代 A/C/D 的证明 | 一次更新的奖励/误差图已完成，同时标出本题零答案可获高分的基线；不得由更新前轨迹推断训练后能力 | 本次执行图已完成，长期收益未验证 |

每项改动必须记录：明确的输入契约；数学恒等式、可行域等价关系或反例；改动的真实生产调用路径；独立参照和必要测试；对原任务的精度与资源影响；适用范围和剩余证明义务。使用参考枚举/解析导数作审计，不能把它接成影子训练算法。

优先复用已有正确内核。`_ExactResponseLP`、运输顶点枚举、终端事件商空间、上下文连通分量、森林运输、SCIP 定价都已经存在，不能重新包装为新发现。先查历史优化记录，避免重复无收益的消元顺序、局部运输界和独立分层方案。

官方 DAPO 的标准化优势、动态组筛选、token-mean 聚合、overlong shaping 和已显式选用的官方 Token-TIS 需要分别说明与原始环境质量目标的关系。保持其官方实现，不能为了使它等于另一个目标而悄悄换成 RLOO 或自写损失。比特级概率相等不是跨推理/训练数值后端正确性的必要条件；应核对 token 对齐、采样概率语义、实际重要性权重及梯度路径。

本轮进展：A 已得到并合入精确结构化简：无 X→Y 直边的单中介链，在二态中介（左右事件相同或不相交）、或三态中介且二元终端事件商空间时，运输对角的极值可同时达到，故整体转为现有运输 LP＋完整响应 LP。共享上下文的机制保持一致。三节点 [0,2/3] 题已由此解决；74 道冻结题仍是 51 成功，无成功状态退化，4 道适用题合计 8.28→1.60 秒，一题从 epsilon_sharp 升至 exact。通用外界候选因导致第 7 题退化未合入。A 的其余 23 道未认证题及 A2 的符号构造问题继续保留。

B 的前期记录：官方 Token-TIS＋DAPO 组合的解析 loss/梯度核对，1、2、4 行 microbatch 划分一致，最大梯度误差约 `1.11e-16`。此处仅为公式验收；后续真实模型更新结果见下文 run-02。服务器证据目录为 `/home/chen/runs/rl-correctness-goal-20260907`，详见其中 `response-envelope-proof.md`、`diagonal-transport-acceptance-summary.json`、`official-dapo-validation-matrix.md`。

当前活动任务：C/C2，在已证明的一般密度与强反转必要条件上继续减少生成核的无用计算。B/E 的隔离一次更新已完成；未触发分支和长期能力收益仍需单独验收。A/A2 和 D 的未解决项保留。

未解决项不会因日志变绿或预算临近耗尽自动完成。若某要求不可同时满足，应给出它们的确切定义、矛盾推导及可选的契约改法；若只是当前算法未解决，应报告剩余证明义务和实际失败证据，不声称理论上不可解。

后续推进记录（2026-09-07）：B 的真实一次更新暴露了固定官方 DAPO recipe 的多轮掩码覆盖错误。`compute_kl_related_metrics` 无条件用 attention mask 重写工具循环的 response_mask，11,437 个动作 token 被扩成 14,413 个，2,976 个工具 token 进入优势和损失分母。源码覆盖点和实际调用张量均已保存；第一次逐函数检查遗漏了 rollout→actor 掩码不变量，其临时“通过”文件已撤销并明确标记为不完整，检查点已判定不能用于后续初始化。不能把局部公式正确或进程退出 0 当作整条训练路径正确。

修复采用固定官方 RayPPOTrainer 已有的 guard：仅在缺少 response_mask 时用 attention mask 回退，保留已有动作掩码。仍从官方 `dapo.main_dapo` 启动；新 recipe 副本位于 `/home/chen/vendor/dapo-official-20260906/verl-recipe-mask-v1`，原副本保留。仅此数据接口改动进入显式补丁清单，优势、组筛选、loss、Token-TIS 与优化器公式均保持上游实现。新增三项实际 recipe 方法测试通过，来源验证增加固定补丁哈希约束，七项来源测试通过。第二次全新基座隔离更新记录位于 `official_execution/run-02-mask-v1-audit-only`，已完成下文所述有限配置验收。

C/C2 已推进到可认证的参数切片：固定其余机制后，一个根 CPT 的两态质量比例使所有动作的干预值和观察联合概率成为一次函数，动作排序与强反转阈值的变化由一次/二次方程完全决定。有理数根隔离参考实现按生产阈值通过 72 个世界、144 个切片、4,032 次独立枚举比较；最大未分类质量约 `8.11e-24`。这也明确了直接条件抽样必须保留的 `q(θ)L(θ)` 密度权重：仅把根概率抽到可行区间里会错误地放大薄切片。二态 ET-V2 ANOVA 系数换元及加权椭球支持经过 150 次实际内核核对、二维/三维符号 Jacobian 检验；多态密度已在本轮补齐，有效提案仍待推进。参考计算未改变生产采样律。

D 已把原弱边反例推进到最优合法实验定理：纯观察及 12 个单点干预下，所有单变量边缘相同；其他有信息实验的似然比仅依赖 `(X,Z)`，故以每样本 2 个标量测量该对变量最优。真实协议收费已核对。B=131072、δ=1/10000 时，最佳可达等先验平均错误率约 **48.9788%**，精确公式为两个二项分布尾概率之和的一半，离散阈值用整数幂认证；更简单的 Pinsker 下界为 48.72%。这是特定反例的所有合法策略最优风险，仍不是题库中困难任务的比例。总体预算校准继续未完成。

上述根因证据归档于 `research/rl_correctness_20260907`，包括切片、二态密度、最优合法实验和第一次运行的掩码覆盖反例。第二次真实掩码传播验收及奖励/误差可视化已完成；接下来把 C 的解析条件变成有确定效率收益、密度正确的生产改进。A/A2 的剩余问题继续保留。


2026-09-07 本轮实际执行验收完成：修复后的 run-02 完成一次全新基座官方更新，质量逐条传入奖励，rollout→actor→四个 loss microbatch 的动作掩码、优势和重要性权重逐项对齐；19,830 个动作 token 进入全局分母，4,018 个工具 token 排除。248 个 LoRA B 张量完成一次与解析 Adam 首步一致的更新，760 个冻结张量无梯度。详见 `dapo-execution-audit-20260907.md` 及其中实际验收 JSON。该有限配置的一次执行检查通过，B/E 不再处于“尚无修复后实际更新”的状态；跨分支、任务分布与长期收益仍不由这一次运行保证。

本次最高质量 0.95531 的真实答案为零向量，而隐藏 ATE 的总变差幅度仅 0.01355869。必须纠正原先含混的归因：强反转筛选只用于 best_intervention，ATE 未经此筛选，不能把这道 ATE 当作筛选失效。已从结果机制证明 `ATE_TV ≤ tanh(t D_Y/4)`，并推导零向量高分的充分强度区间；48 个合法任务和评分核对通过。该界解释弱结果机制为何产生高分常数答案，不代表总体比例，也没有排除路径相消等其他原因。A/A2 的 23 道未认证题、D 的总体预算校准和 C 的密度正确高效提案继续保留。

2026-09-07 本轮 C/C2 验收：一般混合状态数 ET-V2 的块维数、尺度、加权椭球支持、相对于活跃子空间的密度及基准 CLR 密度已由换元证明；43 个精确有理基向量、符号 Jacobian 和 96 个真实机制支持核对通过。ANOVA 投影改为交换的逐轴投影，保持独立高斯块、Dirichlet 能量、随机调用和任务接受条件。64 个世界最大 CPT 差异 `1.89e-15`；25 道完整任务生成 8.879→7.121 秒；五父节点五态投影 0.437→0.073 秒，峰值 Python 分配约减半。收益仅对应所测固定样本，不是训练吞吐或所有配置的保证。详见 `et-v2-projection-and-density-20260907.md` 及 `research/et_v2_projection_20260907/README.md`。下一项可证明改进优先考察强反转绝对因果差阈值所蕴含的内核必要条件，以减少失败候选的推断计算，不改变接受事件。

代码保存已纳入原 GitHub 仓库及 `codex/rl-correctness-official-dapo-20260907` 分支；本地正式副本为 `credit/self-play-in-the-causal-world`，不再依赖 audit 中的无独立 Git 镜像。代码、数学证明、复现程序、失败记录及通过结果及时提交推送；原始模型/梯度张量仍在审计服务器。见 `repository-maintenance.md`。

2026-09-07 本轮继续推进 C/C2：强反转生成的调用证据显示，20 道完整任务中最小后门调整集大小调用 32,667 次、逐子集分离检查 297,950 次。已将这个组合枚举严格化为后门图的端点祖先道德图上的整数顶点割。禁用原图的干预后代，但保留这些节点参与路径；以干预节点的父节点数作为有效割上界。6,550 个小图因果角色及重标号与独立活跃路径枚举严格相同；另有禁用共享瓶颈的反例验收。96 个结构、五类任务的 480 个角色列表相同；20 道最优干预题 26.883→18.935 秒，25 道全部类型任务 7.017→4.791 秒，完整 CPT 与提案序列均相同。详见 `backdoor-minimum-cut-20260907.md` 和 `research/backdoor_mincut_20260907`。

这项改动消除了结构核的组合计算，没有改变最小基数的定义、任务分布或强反转接受概率；更不等于有限预算难度已校准。原全部包含极小调整集枚举及“预测集到合法集的最小 Hamming 距离”要求不同，不能不经证明替换成这个最小基数。下一步可继续把干预值范围给出的强反转必要条件用于省略无用观察推断，并检查对剩余 Hamming 距离问题是否存在有严格条件的等价改法；保持 A/A2、D、官方 DAPO 跨分支和长期收益的剩余义务。
