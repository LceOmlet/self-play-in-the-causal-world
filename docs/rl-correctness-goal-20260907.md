强化学习正确性、环境判别性与效率：持续推进 Goal

长期目标：环境任务有价值，奖励能区分目标能力，任务在真实查询、精度和预算契约下成立，模型通过完整官方 verl DAPO 学会可迁移的解题能力，并以可承担的资源完成训练。以 audit/kernel_integration_20260907/内核修复与验收报告.md 为已验证基线。整个 goal 仍未完成。

最新用户要求：训练与研究并行；坚决避免精确主义和严格证明偏差，必须归纳对全局任务的影响。大问题还必须有明确可改机制；长跑资源量级改不了就接受约束，不强求优化，不再退回局部微优化。先说明影响哪个目标、在实际任务中有多大、能改什么及会改变什么决策，再投入相应深度。证明与检查提供支撑，不能把局部定理、更多同类反例或校验文件当作项目收益。

当前全局判断：已有任务包含可计算的观察／因果差异，但整个分布的能力判别性仍有限；同题验证尚未显示整体学习收益，不能认定不可学。主线是任务价值、有效学习信号及其中可明确改进的大问题；生成耗时高先作为资源约束。训练继续，既有第 50 步验证是观察点，不是可学性的终局裁决或新增停止阈值。具体归纳见 global-task-impact-20260908.md。

| 根因归并 | 对全局任务的影响与当前优先级 | 已有证据 | 下一步及剩余验收 |
|---|---|---|---|
| B/E：官方训练与实际学习 | 当前主线。执行正确是必要条件；能完成更新、得到正奖励不等于学会因果解题 | 官方来源和实际奖励→动作掩码→优势→loss→梯度→Adam 路径已核对；掩码覆盖及进度恢复缺陷已修复。基座／第 25 步固定 25 题尚无整体收益 | 继续训练，读取完整第 50 步同题结果，比较完成率、严格正确、数值误差与解法变化；沿新证据定位修复。继续覆盖未实际触发的分支及长期行为，不把一次执行扩大为全局保证 |
| B/E：实际生成与学习成本 | 当前作为资源约束，只有发现影响大且可消除的机制才升级。耗时占比不是可优化空间的证据 | 同模型等工作量编译对照已提速 2.58–6.59 倍并投入训练；最近 30 步生成仍占 86%，尚未据此发现新的大幅改进方案 | 不因长跑成本高另开微优化主线。若有实际瓶颈及高收益改法再处理；保持官方算法、任务和目标语义，不以少做任务、更短但错误的轨迹或降低验证要求冒充改进 |
| C/C2：节点、DAG、CPT、ET-V2 到条件任务生成 | 决定任务分布与能力判别；目前不是已验证的训练失败主因。接受率、强反转和最终分布偏移属于同一问题 | ANOVA 等价投影、最小后门集整数割、强反转范围提前拒绝已合入并有生成收益；原强反转对照遗漏强观察排名基线，已更正 | 保留正确采样密度，核对收益、后门语义、结构／数值多样性及最终分布。后续改法须改善实际能力判别或重要生成成本；不用比例、排名配额或阈值外壳掩盖根因 |
| A/A2：反事实全局认证与稠密响应计算 | 主要影响反事实题覆盖与分布；未认证题不进入当前可信标签，不自动阻断已认证题训练。它仍是实质内核缺口 | 三节点 [0,2/3] 适用链已修复；74 冻结题中 51 成功、23 未认证。第 43 题消息可压缩，但候选未增加认证且部分成本退化，未合入 | 从共用响应机制和压缩消息入手求等价表示或有效全局界；保持联合可实现性和 Markovian 契约。核对独立精确参照并完整重放 74 题，报告新增覆盖与实际成本，不能只报中间表变小 |
| D：合法实验、答案精度与预算 | 决定有价值的题是否在公开信息和资源下可解；不能用最坏情形冒充全库难度 | 有合法干预解题见证及弱边边界证据；原 NumPy 策略并非最优，部分轨迹超过模型上下文；总体校准未完成 | 按实际合法查询、收费、可达上下文、答案误差和事实事件概率判断。优先解释真实分布中的重要失败，不扩展罕见构造；禁止用 CPT 参数数目、预算档位或配额假装校准 |
| 持续整合：全部工作 | 保证结论与代码可追溯，避免改坏或丢失；这是支撑工作，不替代能力与效率进展 | 本地／服务器仓库及 GitHub 分支持续维护；原始轨迹、必要测试与来源指纹已归档 | 验收通过及时合入；按根因维护前提、改法、证据、实际收益和剩余义务。失败候选如实保留，不将准备、测试通过或进程存活等同于完成 |

必须保留的关键前提：A/A2 中同一节点的跨上下文响应须由同一个联合机制实现，不同节点保持 Markovian 独立；不能用任意独立成对优化替代。第 43 题仍有大规模共享上下文响应约束。复用已有 _ExactResponseLP、运输表示、终端事件商空间、上下文分解与定价能力，避免重复已失败的局部界。

C/C2 的条件生成必须包含可行切片质量的密度权重；固定其余机制后抽到可行参数，并不自动得到原条件分布。已推导的 q(θ)L(θ) 与 ET-V2 换元、支持、密度结论作为改法前提保留。是否值得落地仍由完整提案成本、最终分布和任务价值决定。

B/E 必须保持完整官方 verl／verl-recipe DAPO，无 RLOO、TRL 替换或影子训练循环。奖励计数、终止与 token 掩码、动态过滤、归一化、token 聚合、重要性权重、梯度与实际参数更新均须与真实执行关联。推理／训练概率不逐位相同不能单独判错。部分正确答案获得组内正优势也不自动构成奖励或算法故障。

当前执行使用 Qwen3.5-9B、已验收的编译配置与 progress-v1 官方来源；从原始基座开始的可信训练链已恢复至 heldout-learning-20260908/resume-after-user-hold-01，本轮检查进程仍活跃并已发布第 48 步。图表冻结到第 45 步：52 个生成题组、180 条保留轨迹、174 条提交。后门严格正确 4/40、中介 1/36、最佳干预 10/40；这是不同训练题上的保留样本，不能当作同题学习曲线。来源见 research/training_progress45_20260908/README.md。

当前运行沿用原 500 步／24 小时验证设置，不冒充最终长跑，也不因理论研究或一次观察超时停止。旧的有缺陷检查点弃用；当前已验收训练链的原生检查点继续维护。只有新的明确停止指令或确定的执行正确性故障才重新考虑停训。

最终目标仍为 **10000 次实际官方更新**。剩余问题解决并验收后，最终配置不得设置会提前结束的更小步数、epoch 或墙钟截断；动态过滤和恢复不能导致少更新、跳样本或伪报完成。当前还没有满足完整终态要求，不能以成本换算、少量验证或局部修复提前宣布完成。

既有数学结论、试验记录和历次状态完整保存于 rl-correctness-history-before-global-review-20260908.md，整理前内容 SHA-256 为 4a7c647d1fead826e1e0ba107577d886ca0afc43ab203796d273bce6bd38ef4e。它用于历史追溯；当前优先级与运行要求以本文件为准。局部详细报告继续作为证据，不再在当前清单中逐次叠加旧状态。
