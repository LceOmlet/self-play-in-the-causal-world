Qwen3.5-9B 原生恢复准备与新增真实轨迹，2026-09-08

当前训练仍在第 5 步补采样，已有四次实际更新。原生恢复入口已准备并通过官方配置校验，尚未暂停实际训练、运行 GPU 编译对照或声称恢复成功。完整目标继续保持未完成。

恢复采用官方 `resume_mode=resume_path`，读取第 5 步完整原生检查点，包括 LoRA、Adam、学习率调度器、训练侧随机状态与数据位置。它延续当前验证的 500 次总更新及原始墙钟截止时间；不是用户要求的最终 10,000 次训练。最终仍须修复跨轮次恢复并设置充分 epoch 容量。恢复后的验证改为每次更新保存检查点，并开启官方 vLLM 统计，便于及时检查下一步的实际参数、优化器计数和吞吐；任务、奖励、采样与 DAPO 实现不变。

切换前会检查官方检查点完成标记、496 个 LoRA 张量与对应 Adam 状态、步数和数据位置，再将完整原生文件复制并逐文件核对哈希。第 5 步至多生成 50 批，仍在首轮 500 批内；还会直接比较保存的数据状态恢复后八行与不间断 sampler 的八行。完成这些检查才终止由 PID／创建时间确认的当前训练进程树。终止函数已在独立创建的父子进程上实测，旁边的独立进程与当前真实训练均未受影响；该测试不等于实际训练已经暂停。

目前两套恢复配置都通过官方 Hydra 与 `validate_config`，实际命令和完整配置差异已保存。首次检查器将派生的 trace 名称及旧配置检查时省略的 Ray 环境项判作额外差异而失败；已按原始启动命令补齐比较基线，失败记录保留。它没有触发模型构建或训练失败。

新增轨迹提供了什么证据

- 第 4 步中介任务：四条答案质量分别为 1、0.225、0.25、0.225。正确轨迹用 27 次动作、6,750 个观测标量，完整识别了中介及顺序；其余答案把过多变量列为中介。这是一组真实模型产生、奖励能够区分对错的轨迹，不能扩大为更新后的能力已提高。
- 第 5 步被过滤的最优干预题：四条都选 `state_2`，原始奖励都为 0.06781524；真正最优是 `state_3`。四个因果值为 0.073497、0.081088、0.091809、0.099797，因此选择的是次优动作。观察总体的最优也为 `state_3`，这道题属于观察／干预一致的那部分，不能拿它证明模型失败于强反转。相同奖励没有组内相对优势，官方 DAPO 因而补采样。
- 第 3 步 ATE 任务：零向量的真实 TV 误差为 0.03262952，观察总体捷径为 0.10714027。四条模型误差约为 0.06609、0.07326、0.07810、0.46120；虽然三条优于观察捷径，全部仍劣于零向量。这里不能用“超过观察法”声称已掌握目标能力。

前 16 条保留轨迹均按完整命令序列与原始环境记录唯一匹配。真值来自训练提交前已经认证的同一批文件；没有重新筛题、改奖励或将未完成轨迹计为错误。上述判断与完整快照、恢复准备和失败记录一起保存在 `research/inference_efficiency_20260908/runtime-switch-preparation.tar.gz`。
