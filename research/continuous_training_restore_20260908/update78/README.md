# 持续新题恢复后的首个实际更新

已完成原生 **U77 → U78**，生成计数 **G86 → G87**。这是已保存检查点与完成轨迹的验收；只能确认恢复后确实更新了参数，不能据此声称解题能力已经提高。

- CPU 比较保全的 `global_step_77` 与新 `global_step_78`：496 个 LoRA 张量全部有限且均有元素变化；A 改变 51,086 个元素，B 改变 10,660,640 个元素，总参数差 L2 为 `0.0008324973670866303`。未下载权重，也未执行模型推理或优化器更新。
- 新任务逻辑索引 86、样本种子 4,000,000，家族为 ATE。更新 78 的四条轨迹输入相同，全部提交答案且未超长；最终得分为 `0.236501 / 0.236501 / 0.895178 / 0.493216`。
- 保存的原生 sampler 与 dataset 均指向下一索引 87，dataset 的前一条记录哈希与 journal 第 86 行一致。检查时 journal 已到 `next_index=88`，即下一道题已记录；这与检查点落后于 journal 的恢复契约一致。
- 下一道 CF 为逻辑索引 87、样本种子 4,000,001。缓存精确区间为 `[0.014446021744152718, 0.18699401790606213]`，`terminal_truth_json` 同时保存在 journal 原始题及转发工具的 `row_json` 中，内容完全一致。本次只读核对，没有重新计算其真值。

`acceptance.json` 包含差分摘要、原生数据状态、journal 前缀检查和来源 SHA256。`inspect_saved_update.py` 是实际执行的只读 CPU 脚本，其 SHA 与报告一致。四份原始小产物的本地 SHA 与服务器一致：`data.pt`、`dapo_progress.json`、`78.jsonl` 及 `00000000000000000087.json`。Ruff 检查通过。

检查点来源：`/home/chen/runs/continuous-training-restore-20260908/resume-stream-02/checkpoints/global_step_78`；比较基线：同目录树下的 `hold-01/global_step_77`。本次没有停训、生成额外题或重复算法路径测试。
