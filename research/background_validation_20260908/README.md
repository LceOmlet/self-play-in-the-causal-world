取消全部额外验证，继续官方训练

最终用户决定：不做额外验证，只看训练曲线。同步固定验证已关闭，借用训练 vLLM 的 CPU 验证原型也已退出；没有挂入 DAPO 的训练循环，没有启动周期后台验证。后续不再推进该原型，除非用户重新要求。

第 50 步原生检查点先复制并逐文件核对，再取消阻塞验证。沿用已有控制器和完整官方训练实现，仅将 test_freq 从 25 改为 -1，val_before_train 仍为 false，其他配置差异为运行目录。原 500 步检查阶段与墙钟截止时间保持原值，最终 10000 次实际更新目标未改。

恢复时核对 496 个 LoRA 参数、496 份 Adam 状态及 992 个有限矩、scheduler=50；调用官方数据恢复，接续生成批次 58，下一批行号与连续采样一致。首次启动因 Ray Unix socket 路径过长在 GPU 初始化前退出，改用短运行名 resume-no-val-01 后恢复成功。实际第 51 次更新已完成并发布原生进度：completed_updates=51、generated_batches=59，梯度范数 0.16015625。生产算法、环境、奖励和任务生成均未修改。

`stop_blocking_validation.py`、`resume_without_periodic_validation.py` 是已有控制脚本的最小改编。原脚本不改，以保留此前证据的源码身份。执行时副本保存在证据包中；停训脚本后续仅折行，AST 相同。

`withdrawn/` 保存撤回的借用 vLLM 原型，**未验收、未启用**。原设想为独立 CPU 客户端调用官方 ToolAgentLoop 和训练服务器的 generate RPC，以取消自己的请求让路；并无训练器的原子通知协议，不能保证零等待或完整固定验证。5 个本地调度检查通过，不等于生产验收。接入过程中修正了 Ray 同步 RPC 不能直接返回 coroutine 和 parquet 工具参数位置两处问题。用户取消时第二次试接入尚未完成一个生成回合，记录 51 次让路、请求等待累计约 0.64 秒；没有完成任何验证题，没有验证成绩。SIGINT 在客户端等待重试时退出，服务器检查无遗留验证进程，训练进程仍活跃。

`validation-disabled-and-through51-evidence.tar.gz` 保存 45 个来源文件，包括停训／恢复元数据、原生 CPU 恢复结果、官方来源校验、两段训练日志快照和撤回原型记录；不包含大体积模型状态。归档 SHA-256：`78189743948fab2841c32b25eb536258df834b2891a6ed5473cf6191e53eb3dc`，620041 字节。逐文件身份见 `validation-disabled-evidence-manifest.json`。服务器原生检查点仍保全。

第 50 步固定验证为用户取消的部分过程，不能填成零分或完整结果。未来观察方式以训练曲线为准，见 `../training_progress51_20260908/README.md`。
